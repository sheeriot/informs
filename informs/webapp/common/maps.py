from icecream import ic
ic.disable()


def staticmap_aid(instance, aid_loc):
    # build a static map for the aid request
    # get the field op location
    try:
        # ... existing code ...
        except httpx.RequestError as e:
            print(f"An error occurred while requesting {e.request.url!r}.")

def create_static_map(instance, aid_loc=None):
    # get the aid request
    aid_request = instance
    if aid_loc is None:
        # ... existing code ...
